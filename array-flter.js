const words = ['spray', 'limit', 'elite', 'exuberant', 'destruction', 'present'];


for (let i = 0; i < array.length; i++){
    x = words[i];
        
    x.length>8 ? newArray.push(valeur):console.log("pas trouvé");
    console.log(newArray);
  }
  